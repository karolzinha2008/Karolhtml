// Controla a velocidade do letreiro
const letreiro = document.querySelector('.letreiro');

// Definindo a velocidade da animação, que é de 12 segundos
const velocidade = 20;

// Atribui a duração da animação ao letreiro
letreiro.style.animationDuration = `${velocidade}s`;
